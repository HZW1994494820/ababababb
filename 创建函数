#include <stdio.h>
#include <string.h>
int get_max(int x, int y)//定义函数
{
	if (x > y)
	{
		return x;
	}
	else
	{
		return y;
	}
}
int main()
{
	int a = 100;
	int b = 20;
	int max = get_max(a, b);//函数的使用
	printf("%d\n", max);
	return 0;
}
