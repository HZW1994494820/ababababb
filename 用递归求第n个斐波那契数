#include<stdio.h>
int fib(int n)
{
	if (n<=2)
	{
		return  1;
	}
	else if (n > 2)
	{
		return fib(n - 1) + fib(n - 2);
	}
}
int main()
{
	int n = 0;
	scanf_s("%d", &n);
	int len = fib(n);
	printf("%d\n", len);
}
